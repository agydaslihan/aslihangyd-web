node_modules/
.next/
out/
.env
.env.*
# ⚠️ Örnek dosyalar İSTİSNA — yalnızca yer tutucu içerirler ve depoda
# olmaları gerekir. `.env.*` deseni ikisini de yutuyordu; üretim örneği
# yazıldığı gün commit edilemiyordu ve fark edilmesi tesadüfe kalmıştı.
!.env.example
!.env.production.example
*.log
.DS_Store
dist/
build/
coverage/
.claude/settings.local.json

# TypeScript artımlı derleme önbelleği
*.tsbuildinfo
next-env.d.ts

# Payload medya yükleme dizini (git dışı)
medya/
docker/certs/
