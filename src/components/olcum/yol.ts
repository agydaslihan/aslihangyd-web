/**
 * Gözlemlenebilirlik ekranının adresi.
 *
 * ⚠️ Ayrı dosyada, çünkü `payload.config.ts` ile gezinme bağlantısı aynı
 * değeri okumalı. İki yerde yazılsaydı yol değiştiğinde bağlantı sessizce
 * 404'e giderdi.
 */
export const OLCUM_YOLU = '/gozlemlenebilirlik'
